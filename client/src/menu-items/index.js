// project import
import dashboard from './dashboard';
import support from './support';
import tools from './tools';
import reports from './reports';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, reports, tools, support]
};

export default menuItems;
